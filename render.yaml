services:
  - type: web
    name: evsu-fleet-tracker
    env: python
    region: singapore
    plan: free
    buildCommand: >
      pip install -r requirements.txt &&
      python manage.py collectstatic --noinput &&
      python manage.py migrate
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --log-file -
    envVars:
      - key: DEBUG
        value: 'False'
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: 'evsu-fleet-tracker.onrender.com'
      - key: DATABASE_URL
        fromDatabase:
          name: evsu-fleet-tracker-db
          property: connectionString
